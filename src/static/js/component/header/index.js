const header = document.getElementById("menu_nav_header");

window.addEventListener("scroll", () => {
  console.log(header);
  //   if (window.scrollY > 700) {
  //     header.style.position = "fixed";
  //   }
});
